{{#each product}}
	<div class="container mx-auto card">
		<div class="row">
			<div class="col-md-4">
				<img src="{{thumbnail}}" class="card-img-top" alt="{{thumbnail}}" />
			</div>
			<div class="col-md-8">
				<h1>{{name}}</h1>
				<p>Price: {{price}}</p>
			</div>
		</div>
	</div>
{{/each}}