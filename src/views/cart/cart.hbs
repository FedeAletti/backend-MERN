<div>
	<h1>Hola</h1>
	{{!-- {{#if productsExist}}
		{{#each products}}
			{{#if @first}}
				<div class="cart-item">
					<div class="cart-item__name">Name</div>
					<div class="cart-item__price">Price</div>
					<div class="cart-item__quantity">Quantity</div>
					<div class="cart-item__total">Total</div>
				</div>
			{{/if}}
			<div class="cart-item">
				<div class="cart-item__name">{{name}}</div>
				<div class="cart-item__price">{{price}}</div>
				<div class="cart-item__quantity">{{quantity}}</div>
				<div class="cart-item__total">{{total}}</div>
			</div>
		{{/each}}
	{{/if}} --}}

</div>